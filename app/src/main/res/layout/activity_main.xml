<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingStart="@dimen/row_inset"
        android:paddingEnd="@dimen/row_inset"
        android:paddingBottom="@dimen/home_list_padding_bottom" />

    <!-- The first run, in place of the list rather than at the top of it.
         ScrollView because the block has to survive landscape and the largest system
         font size; fillViewport because without it the child would only be as tall as
         its contents and gravity="center" would have nothing to centre within. -->
    <ScrollView
        android:id="@+id/welcome"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingStart="32dp"
            android:paddingTop="24dp"
            android:paddingEnd="32dp"
            android:paddingBottom="@dimen/home_list_padding_bottom">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="@string/welcome_headline"
                android:textAppearance="?attr/textAppearanceHeadlineSmall" />

            <GridLayout
                android:id="@+id/formatGrid"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="28dp"
                android:columnCount="@integer/welcome_grid_columns"
                android:focusable="true" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="28dp"
                android:gravity="center"
                android:lineSpacingExtra="2dp"
                android:text="@string/welcome_promise"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textColor="?attr/colorOnSurfaceVariant" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/openFileButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:minWidth="@dimen/welcome_action_min_width"
                android:text="@string/open_a_file" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/addFolderButton"
                style="@style/Widget.Material3.Button.OutlinedButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:minWidth="@dimen/welcome_action_min_width"
                android:text="@string/add_folder" />
        </LinearLayout>
    </ScrollView>

    <!-- Floating glass chrome. Content scrolls underneath; the fill is
         translucent so the list remains visible at the edges. -->
    <LinearLayout
        android:id="@+id/chromeHost"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:orientation="vertical"
        android:paddingStart="@dimen/glass_margin"
        android:paddingEnd="@dimen/glass_margin">

        <FrameLayout
            android:id="@+id/toolbarGlass"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@drawable/bg_glass_panel"
            android:clipToOutline="true"
            android:elevation="@dimen/glass_elevation"
            android:outlineProvider="background">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/transparent"
                app:elevation="0dp"
                app:titleTextAppearance="?attr/textAppearanceTitleLarge" />

            <LinearLayout
                android:id="@+id/lockup"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:visibility="gone">

                <ImageView
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:importantForAccessibility="no"
                    android:src="@mipmap/ic_launcher" />

                <ImageView
                    android:id="@+id/wordmark"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="9dp"
                    android:contentDescription="@string/app_name"
                    android:src="@drawable/ic_wordmark"
                    app:tint="?attr/colorOnSurface" />
            </LinearLayout>
        </FrameLayout>

        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/loadProgress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:indeterminate="true"
            android:visibility="gone"
            app:trackCornerRadius="2dp" />
    </LinearLayout>

    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/openFab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="@dimen/home_fab_margin_bottom"
        android:text="@string/open_a_file" />

    <FrameLayout
        android:id="@+id/pillNavHost"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:padding="@dimen/nav_pill_margin">

        <com.arjun.gander.PillNavBar
            android:id="@+id/pillNav"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:background="@drawable/bg_nav_pill"
            android:elevation="@dimen/glass_elevation"
            android:outlineProvider="background"
            android:padding="@dimen/nav_pill_padding" />
    </FrameLayout>
</FrameLayout>
